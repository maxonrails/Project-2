Title: <%= @post.title %><br>
Body: <%= @post.body %><br>

Choices: <% @post.post_items.each do |post_item| %>

<%= post_item.votes.count %>

<%= link_to post_item.item.name,votes_path(:post_item_id => post_item.id, :user_id => current_user.id),class: 'btn btn-info btn-xs' %>

<% end %>