<%= simple_form_for @post, remote: true do |f| %>

<div>
     <%= f.label :title %>
     <%= f.text_field :title %>
</div>

<div>
    <%= f.label :body %>
    <%= f.text_field :body %>
</div>
<%= f.collection_check_boxes :item_ids, Item.all, :id, :name %>

 <%= f.button :submit %>
<% end %>